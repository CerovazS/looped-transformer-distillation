_target_: loopdistill.data.datamodule.TrajectoryDataModule
train_manifest: ${paths.manifest_path}
val_manifest: ${paths.manifest_path}
test_manifest: ${paths.manifest_path}
batch_size: 4
num_workers: 0
pin_memory: false
drop_last: false
